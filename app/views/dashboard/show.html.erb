<div class="my-container">
  <section class="my-box full-page-width dashboard intro">
    <header>
      <h1>Welcome to noobHub!</h1>
    </header>
    <div class="my-content">
      <%= image_tag @big_hairy.profile_pic %>
      <p>I'm <%= @big_hairy.github_username %> (legally <%= @big_hairy.name %>) and I am currently
        at <%= @big_hairy.job %> in <%= @big_hairy.location %>. I have <%= @big_hairy.contributions %> contributions
        this
        year, my longest commit streak was <%= @big_hairy.longest_streak %> days, and my current streak
        is <%= @big_hairy.current_streak %> days.</p>
    </div>
    <footer>
      <span><%= @big_hairy.starred_repos %> Starred Repos</span>
      <span><%= @big_hairy.followers.count %> Followers</span>
      <span><%= @big_hairy.following.count %> Following</span>
    </footer>
  </section>
</div>

<div class="my-container">
  <section class="my-box full-page-width dashboard">
    <header><h2>Repositories</h2></header>
    <% @big_hairy.repositories.each do |repository| %>
      <div class="my-box third-page-width small-box">
        <h4><%= link_to repository['name'], repository['url'] %></h4>
      </div>
    <% end %>
  </section>
</div>

<div class="my-container">
  <section class="my-box full-page-width">
    Some more crap
  </section>
</div>
